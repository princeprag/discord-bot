/**
 * Defines the config keys that are stored as arrays.
 */
export type ArraySettingsType =
  | "hearts"
  | "blocked"
  | "self_roles"
  | "anti_links"
  | "permit_links"
  | "link_roles"
  | "allowed_links"
  | "level_roles";
